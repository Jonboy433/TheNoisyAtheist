<div id="content">
<label>Name:</label><%= @user.user_id %><br></input>
<label>Email:</label><input type='text' value='<%= @user.email %>'><br>
<label>Last sign in: </label><%= @user.current_sign_in_at.strftime('%a %m/%d/%y %l:%M%p') %><br>
</div>

<div id="user_history">
    <h2>Blog History</h2>
    <div id="blog_history">
    <%= render @user.blogs.order('created_at') %>
    </div>
    <div id="comment_history">
    <h2>Comment History</h2>
    <p>Comment count: <%= @user.comments.count %>
    </div>
</div>